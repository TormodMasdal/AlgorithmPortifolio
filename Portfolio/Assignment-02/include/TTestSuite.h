#pragma once
#include <string>

/**
 * @brief Simple test runner that handles menu navigation and demos.
 */
class TTestSuite {
public:
    /// @brief Starts the interactive menu used to test the assignment.
    static void RunAll();
};
